[build-system]
requires      = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "File2Img"
version = "0.1.0"
description = "A tiny GUI tool that embeds any file into a PNG image and vice versa, powered by Flet."
readme = "README.md"     
requires-python = ">=3.8"
dependencies = [
    "flet>=0.11.0",
    "Pillow>=9.0.0",
    "pyjnius>=1.4.0",
    "android>=0.0.0; platform_system=='Android'",
]

[project.scripts]           
fti = "main:main" 

[tool.hatch.build.targets.wheel]
packages = ["."] 

[tool.flet.android]
min_sdk_version = 24
# Include pyjnius so that flet build apk bundles JNI bridge and allow SAF calls
dependencies = [
    "pyjnius"
]

[tool.flet.flutter.pubspec.dependency_overrides]
webview_flutter_android = "4.10.1"

